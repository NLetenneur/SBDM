{% extends 'base.html.twig' %}

{% block title %}Ticket{% endblock %}

{% block body %}
    <h1>Ticket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Annee</th>
                <td>{{ ticket.annee }}</td>
            </tr>
            <tr>
                <th>NumeroTicket</th>
                <td>{{ ticket.numeroTicket }}</td>
            </tr>
            <tr>
                <th>DateVente</th>
                <td>{{ ticket.dateVente ? ticket.dateVente|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a role="button" class="btn btn-light mb-2" href="{{ path('ticket_index') }}">Retour à la liste</a> <br>

    <a role="button" class="btn btn-info mb-2" href="{{ path('app_ticket_edit', {'annee': ticket.annee}) }}">Modifier</a>

    {{ include('ticket/_delete_form.html.twig') }}
{% endblock %}
