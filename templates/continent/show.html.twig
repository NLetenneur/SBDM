{% extends 'base.html.twig' %}

{% block title %}Continent{% endblock %}

{% block body %}
    <h1>Continent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdContinent</th>
                <td>{{ continent.idContinent }}</td>
            </tr>
            <tr>
                <th>NomContinent</th>
                <td>{{ continent.nomContinent }}</td>
            </tr>
        </tbody>
    </table>

    <a role="button" class="btn btn-light mb-2" href="{{ path('app_continent_index') }}">Revenir à la liste</a><br>

    <a role="button" class="btn btn-info mb-2" href="{{ path('app_continent_edit', {'idContinent': continent.idContinent}) }}">Modifier</a>

    {{ include('continent/_delete_form.html.twig') }}
{% endblock %}
