{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdArticle</th>
                <td>{{ article.idArticle }}</td>
            </tr>
            <tr>
                <th>NomArticle</th>
                <td>{{ article.nomArticle }}</td>
            </tr>
            <tr>
                <th>PrixAchat</th>
                <td>{{ article.prixAchat }}</td>
            </tr>
            <tr>
                <th>Volume</th>
                <td>{{ article.volume }}</td>
            </tr>
            <tr>
                <th>Titrage</th>
                <td>{{ article.titrage }}</td>
            </tr>
        </tbody>
    </table>

    <a role="button" class="btn btn-light mb-2" href="{{ path('article_index') }}">Retour à la liste</a> <br>

    <a role="button" class="btn btn-info mb-2" href="{{ path('app_article_edit', {'idArticle': article.idArticle}) }}">Modifier</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}